<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_inscripcion" android:icon="@android:drawable/ic_menu_add"
            android:title="Inscripcion" />
        <item android:id="@+id/nav_calendario" android:icon="@android:drawable/ic_menu_week"
            android:title="Calendario" />
        <item android:id="@+id/nav_cursos" android:icon="@android:drawable/ic_menu_agenda"
            android:title="Cursos" />
    </group>

    <item android:title="Otros">
        <menu>
            <item android:id="@+id/nav_buscar" android:icon="@android:drawable/ic_menu_search"
                android:title="Buscar" />
            <item android:id="@+id/nav_enviar" android:icon="@android:drawable/ic_menu_send"
                android:title="Enviar" />
        </menu>
    </item>

</menu>